/**
 * Forms demo stub — form controls demo lands in #138.
 */

import { AppShell } from '../../components/app-shell';

const BASE_PATH = '/pj/zudo-design-token-panel/examples/zfb/';

export default function FormsPage() {
  return (
    <AppShell
      title="Forms — zfb — Design Token Panel"
      activePath={`${BASE_PATH}components/forms/`}
    >
      <h1 class="zfbexample-heading">Form controls demo</h1>
      <p>Form controls demo lands in #138.</p>
    </AppShell>
  );
}
