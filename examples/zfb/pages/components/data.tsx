/**
 * Data demo stub — data and media demo lands in #138.
 */

import { AppShell } from '../../components/app-shell';

const BASE_PATH = '/pj/zudo-design-token-panel/examples/zfb/';

export default function DataPage() {
  return (
    <AppShell
      title="Data — zfb — Design Token Panel"
      activePath={`${BASE_PATH}components/data/`}
    >
      <h1 class="zfbexample-heading">Data and media demo</h1>
      <p>Data and media demo lands in #138.</p>
    </AppShell>
  );
}
